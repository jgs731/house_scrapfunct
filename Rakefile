task :default => :csv

desc 'Run Planning Scraper and convert to CSV'
task :csv do |r|
  # run the query in the Planning Scraper class
  # Aside from the header, convert each field on each row
  # Export to CSV for further analysis
end